apiVersion: v1
kind: Service
metadata:
  name: chromadb
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: chromadb
  ports:
    - name: http
      port: 8000
      targetPort: http
      nodePort: 30081
      protocol: TCP
